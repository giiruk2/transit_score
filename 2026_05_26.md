# 2026-05-26 UI 개선 작업 요약

전체 코드 리뷰에서 도출된 **우선순위 높음** 4건과, 사용자 피드백에 따른 **교통수단 아이콘 가독성 보완**을 진행함.

## 작업 범위

| # | 항목 | 결과 |
|---|---|---|
| 1 | 이모지 아이콘 → SVG 라인 아이콘 통일 | ✅ |
| 2 | 키보드 포커스 상태 (`:focus-visible`) | ✅ |
| 3 | 작은 텍스트 대비(WCAG) 강화 | ✅ |
| 4 | JS `onMouseEnter/Leave` → CSS `:hover` 전환 | ✅ |
| + | 사용자 피드백: 버스/지하철 아이콘 구별성 보완 | ✅ |
| 5 | 사이드바 접기/펴기 토글 (localStorage 보존) | ✅ |
| 6 | 컨테이너 크기 변경 시 Kakao Map 자동 relayout | ✅ |
| 7 | git rebase + push (origin/main) | ✅ |

---

## 1. 이모지 → SVG 라인 아이콘

### 배경
이모지(🗺 🛤 👤 ⚙️ 📍 ✏️ ✨ 💡 🔍 🚶 🚌 🚇 🔄 ⏳ ⚠️ 등)는 OS/브라우저별로 색·굵기·기준선이 달라 일관된 디자인을 만들기 어려움.

### 작업
- 신규 [frontend/src/components/icons.tsx](frontend/src/components/icons.tsx) — `currentColor` 기반 SVG 컴포넌트 15종 (24×24 viewBox, `size`/`strokeWidth` props)
  - `IconMap`, `IconRoute`, `IconUser`, `IconSettings` (사이드바 탭)
  - `IconPin`, `IconPencil`, `IconSparkle`, `IconBulb`, `IconSearch` (UI 일반)
  - `IconWalk`, `IconBus`, `IconSubway`, `IconHourglass`, `IconTransfer` (교통수단/GTT 분해)
  - `IconWarning`
- 교체 위치: `page.tsx`, `OriginPanel.tsx`, `AttractionList.tsx`, `SearchBar.tsx`, `ScorePanel.tsx` (총 13곳)

### 가시 변경
- 사이드바 탭 4개가 이모지 → 라인 아이콘으로 통일
- 점수 패널의 GTT 분해 라벨(탑승/도보/대기/환승) 옆 색상 매칭 아이콘
- 경로 옵션의 모드 표시(도보/버스/지하철) 정렬 깔끔
- CTA "내 취향 설정하기"의 ✨ → 4각 별 SVG

---

## 2. 키보드 포커스 상태

### 배경
거의 모든 인터랙티브 요소에 `:focus-visible` 스타일이 없거나, 일부 input은 `outline-none`으로 포커스 표시 자체를 제거. WCAG AA 키보드 접근성 미달.

### 작업
- [globals.css](frontend/src/app/globals.css)
  - 전역 `:focus { outline: none }` (마우스 클릭 시 outline 안 보임)
  - `button:focus-visible, a:focus-visible, input:focus-visible, ...` 에 accent 색 2px outline + 2px offset
  - `.search-input:focus-visible` 는 별도로 더 굵은 링 + 그림자
- [SearchBar.tsx](frontend/src/components/SearchBar.tsx) — `onFocus`/`onBlur` 의 JS boxShadow 토글 제거하고 클래스 위임
- `outline-none` Tailwind 클래스 제거 위치: `OriginPanel.tsx`, `AuthModal.tsx` ×3, `page.tsx` ×2 (인라인 편집기)

### 가시 변경
- Tab 키 탐색 시 액센트 색 포커스 링이 따라옴
- 마우스 클릭으로 포커스된 경우엔 표시 안 됨 (모던 표준)

---

## 3. 텍스트 대비 강화

### 배경
10~11px 작은 메타 텍스트(푸터 "총 N개 관광지", 저장 경로 날짜 등)에 `--panel-text-muted: #475569` 적용. 흰 패널 위에서 가독성이 떨어짐.

### 작업
[globals.css](frontend/src/app/globals.css):
- `--panel-text`: `#1e293b` → **`#0f172a`** (slate-900, 본문 강조)
- `--panel-text-muted`: `#475569` → **`#334155`** (slate-700, 작은 텍스트도 또렷)
- 신규 `--panel-text-subtle: #64748b` (slate-500, placeholder 류 선택 사용)

54개 변수 사용 위치에 자동 반영됨.

### 가시 변경
- 푸터·날짜·카테고리 라벨 등 회색 메타 텍스트가 한 단계 진해짐

---

## 4. JS hover → CSS hover

### 배경
`onMouseEnter`/`onMouseLeave` 로 `style.opacity`나 `style.background`를 토글하는 패턴이 7쌍 존재. 다른 인라인 style과 충돌 가능성, 불필요한 JS 코드.

### 작업
모두 Tailwind `hover:` 변형으로 교체:

| 위치 | 변경 |
|---|---|
| `page.tsx` (저장 경로 카드, 즐겨찾기 카드) | `hover:opacity-80` |
| `AttractionList.tsx` (그리드/리스트 카드) | `hover:opacity-[0.85]` |
| `SearchBar.tsx` (드롭다운 행) | `hover:bg-[var(--panel-surface)]` |
| `OriginPanel.tsx` (저장 출발지 행) | `hover:bg-[var(--panel-surface)]` |
| `MapViewer.tsx` (컨텍스트 메뉴 버튼) | `hover:bg-[rgba(73,180,222,0.1)]` |

### 가시 변경
- 시각적으론 동일 (`transition-all` 그대로 유지)
- 14줄 JS 제거, 충돌 가능성 해소

---

## + 교통수단 아이콘 구별성 보완 (사용자 피드백 반영)

### 배경
경로 옵션 리스트에서 `IconBus`/`IconSubway`가 12px 크기에서는 둘 다 "직사각형 + 가로선"으로 보여 구별이 어려움. `IconWalk`도 stride가 잘 안 드러남.

### 작업
[icons.tsx](frontend/src/components/icons.tsx):
- `IconBus` — 바퀴를 **채운 원**(`fill="currentColor"`)으로 변경, 창문 분할선 추가. 가로 직사각형 = 버스 형태
- `IconSubway` — 세로형 **알약 모양**(rx=4)으로 변경 + 하단 트랙선. 버스와 가로/세로 비율로 한눈에 구별
- `IconWalk` — stride(다리 V) + 팔 두 개 명확히

[ScorePanel.tsx](frontend/src/components/ScorePanel.tsx):
- 경로 옵션의 모드 아이콘 크기 **12 → 14px**, 라벨 폰트 10 → 11px

### 가시 변경
- 버스(가로 직사각형 + 채워진 큰 바퀴) vs 지하철(세로 알약 + 트랙) 시각적으로 명확
- 도보 아이콘이 "걷는 사람" 픽토그램으로 더 또렷

---

## 5. 사이드바 접기/펴기

### 배경
사용자 피드백: 사이드바가 전체 화면의 1/3 이상 차지해 지도 영역이 좁다. 펼침/접힘을 사용자가 직접 제어할 수 있어야 함.

### 작업
[icons.tsx](frontend/src/components/icons.tsx):
- 신규 `IconChevronLeft`, `IconChevronRight`

[page.tsx](frontend/src/app/page.tsx):
- `sidebarCollapsed` state + `localStorage`로 상태 보존 (`'sidebarCollapsed'` 키)
- `toggleSidebar()` — state 토글 + localStorage 동기 저장
- `handleTabClick(id)` — 접힌 상태에서 탭 클릭 시 자동으로 펼치며 해당 탭으로 전환
- `<aside>` 폭을 조건부: `sidebarCollapsed ? '72px' : 'var(--sidebar-width)'`, transition 220ms ease
- 콘텐츠 패널을 `overflow-hidden` 래퍼로 감싸 transition 중 텍스트 reflow 없이 깔끔히 잘림 (inner div는 `width: calc(var(--sidebar-width) - 72px)` 고정)
- 우측 가장자리 floating 토글 버튼 — 원형, accent 색 chevron, top-24 위치, z-20
- 접힌 상태에선 활성 탭 시각 표시 비활성화 (혼동 방지)

### 가시 변경
- 사이드바 우측에 작은 원형 chevron 버튼 항상 노출
- 클릭 시 패널이 72px 탭바만 남기고 부드럽게 접힘 → 지도 ~330px 넓어짐
- 새로고침해도 마지막 상태 유지

---

## 6. Kakao Map 자동 relayout

### 배경
사이드바 접기 후 지도가 새 폭에 맞춰 다시 그려지지 않고 잘려 보임. Kakao Map은 컨테이너 크기 변경 시 자동 redraw를 안 함.

### 작업
[MapViewer.tsx](frontend/src/components/MapViewer.tsx):
- `useEffect`로 `mapInstance.getNode()`에 `ResizeObserver` 부착
- 크기 변화 감지 시 80ms debounce 후 `mapInstance.relayout()` 호출
- cleanup에서 `ro.disconnect()` + `clearTimeout`

### 효과
- 사이드바 transition(220ms) 진행 중에도 ResizeObserver가 여러 번 발화하지만, debounce 덕에 마지막 한 번만 실제 relayout 실행 → 성능 영향 거의 없음
- 추후 다른 외부 폭 변경 (창 크기, 추가 패널 등) 에도 자동 대응

---

## 7. Git: rebase + push to origin/main

### 진행
- 로컬 commit `6db258c` 작성 후 push 시도 → Windows 자격증명에 캐시된 다른 계정(`feelpill`) 때문에 403
- PAT로 인증 통과 후, 원격에 새 commit `7b2e102 feat: 지하철 노선 polyline 데이터 교체 및 환승 제한 적용`이 있어 fast-forward 거부
- `git rebase FETCH_HEAD` 진행 중 `MapViewer.tsx` 충돌 발생

### 충돌 해결 (`getSubwayCoords` 함수)
- **원격 (구 알고리즘)**: 전체 segment 통틀어 board/alight 최근접점을 각각 찾기 → 같은 segment일 때만 slice
- **로컬 (신 알고리즘)**: segment별로 board/alight 거리 합산이 가장 작은 한 곳에서만 slice
- → **로컬 채택** (상/하행이 별도 segment인 경우 한쪽 방향만 깔끔히 그려지는 개선)

### 최종
- 새 commit hash: `4b22c98`
- `7ee7a0f..4b22c98 main -> origin/main`
- origin 트래킹 ref 동기화 완료

---

## 빌드/검증
- TypeScript: 매 단계 `exit 0` (clean)
- Docker frontend 이미지 4회 리빌드 (각 ~14s, backend/otp/redis는 캐시 히트)
- 4개 컨테이너(redis/otp/backend/frontend) 정상 기동

## 잔여 (다음 라운드 후보)
원 리뷰의 **우선순위 중간/하** 항목들:
- 인라인 스타일 토큰화 (`--accent-bg-soft` 등 추가, `rgba(73,180,222,0.15)` 반복 제거)
- 폰트 스케일 단일화 (CSS 토큰 vs Tailwind 임의값 혼재 정리)
- 라운드 반경 2단계로 축소
- 카테고리 플로팅 칩 줄바꿈 처리(좁은 화면)
- 프리셋 이름 입력 UX 개선
- 모바일 브레이크포인트
- 점수 로딩 스켈레톤
- (옵션) 사이드바 드래그 리사이즈 핸들 — 현재는 접기/펴기만 지원
