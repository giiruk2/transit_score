# TransitScore_2 코드 점검 보고서 #2

**일시**: 2026-03-19
**점검 범위**: 백엔드 6개 파일 / 프론트엔드 6개 파일 (총 12개)
**점검 기준**: 버그 / 경고 / PASS
**결과 요약**: PASS 34 / 경고 12 / 버그 4

---

## 백엔드

### `backend/src/app.ts`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 1 | 헬스체크 엔드포인트 | PASS | 정상 구현 |
| 2 | DB 조회 에러 처리 | PASS | try-catch 및 500 반환 |
| 3 | accessScore DB 반영 | PASS | 1단계 버그 수정 완료 |
| 4 | 좌표 NaN 검증 누락 | **BUG** | `parseFloat(originLatStr)` 결과가 NaN이어도 그대로 통과. 잘못된 좌표가 ODsay API로 전달됨 |
| 5 | mockData fallback | 경고 | DB 비어있을 때 mockData 반환 — 운영 중에는 불필요, 제거 권장 |
| 6 | fetchOdsayRoute 에러 미처리 | 경고 | `await fetchOdsayRoute(...)` 실패 시 try-catch 없음, 500 반환됨 |

---

### `backend/src/services/routeService.ts`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 7 | ODsay 경로 파싱 | PASS | bestPath.info 필드 정상 추출 |
| 8 | Fallback 자체 추정 로직 | PASS | 직선 거리 기반 추정치 적절히 구현됨 |
| 9 | BusanBIMS 실시간 대기시간 연동 | PASS | `/stopArrByBstopid` 호출 + min1 반영 완료 |
| 10 | xml2js 파싱 예외 처리 | PASS | try-catch로 실패 시 5분 fallback |
| 11 | **BUSAN_BIMS_KEY 하드코딩** | **BUG** | API 키가 코드 내 상수로 선언됨 (line 15). `.env`로 이동해야 함 |
| 12 | Origin/Referer 헤더 localhost 고정 | 경고 | `'Origin': 'http://localhost:5001'` 하드코딩 — 배포 시 환경변수화 필요 |
| 13 | 환승 계산 가독성 | 경고 | `(sum - 1 > 0) ? (sum - 1) : 0` → `Math.max(0, sum - 1)` 로 단순화 권장 |
| 14 | extractFirstBusStopName 지하철 미포함 | 경고 | `trafficType === 2`(버스)만 탐색, 지하철(1)로만 이동 시 waitTimeMin이 항상 5분 |

---

### `backend/src/services/score.ts`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 15 | 가중합 수식 | PASS | 0.45/0.20/0.15/0.10/0.10 정확히 합산 |
| 16 | 범위 clamp (0~1) | PASS | s_time, s_walk, s_wait 모두 clamp 처리 |
| 17 | accessScoreFactor 기본값 | PASS | 0.5 중립값 적절 |
| 18 | 경계값 문서화 없음 | 경고 | T_best=20, T_worst=120, D_max=1200 등 상수가 매직넘버로 인라인 — 상수 분리 권장 |

---

### `backend/src/scripts/seed.ts`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 19 | enriched_attractions.json 연동 | PASS | accessScore 포함 upsert 정상 |
| 20 | contentId upsert 중복 방지 | PASS | where: { contentId } 기준 upsert |
| 21 | LAT/LNG NaN 검증 없음 | 경고 | `parseFloat(item.LAT)`이 NaN인 경우 DB에 NaN 저장 가능 |
| 22 | 트랜잭션 미사용 | 경고 | 루프 중간에 실패하면 부분 저장됨. `prisma.$transaction()` 고려 |

---

### `backend/src/scripts/scoreAccessibility.ts`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 23 | 키워드 가중합 모델 | PASS | POSITIVE_RULES + PENALTY_RULES 정상 적용 |
| 24 | 공백 없는 화장실 variant 추가 | PASS | `장애인화장실` 키워드 추가 완료 (이번 세션 수정) |
| 25 | 점수 분포 출력 | PASS | 버킷 집계 및 의심 케이스 감지 정상 |
| 26 | 대소문자/공백 민감성 | 경고 | `text.includes(kw)` 는 완전 일치. 트림 없이 `"엘리베이터 "` 등은 매칭 실패 가능 |

---

### `backend/src/scripts/fetchAttractions.ts`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 27 | API 호출 및 저장 | PASS | 공공데이터포털 관광 API 연동 정상 |
| 28 | 파일 저장 에러 미처리 | 경고 | `fs.writeFileSync` 주변 try-catch 없음. 디스크 오류 시 uncaught |

---

## 프론트엔드

### `frontend/src/components/ScorePanel.tsx`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 29 | 점수 렌더링 (rawParams 기반) | PASS | totalTimeMin, transferCount 등 rawParams 사용 — 정상 동작 |
| 30 | 로딩/에러 상태 분기 | PASS | loading/error 상태 정상 처리 |
| 31 | fallback 경고 배너 | PASS | isFallback 시 추정치 경고 표시 |
| 32 | **ScoreDetails.breakdown 타입 불일치** | **BUG** | 인터페이스가 `timeScore`, `transferScore` 등을 정의하지만 백엔드는 `s_time`, `s_transfer`를 반환. 현재 렌더링에서 `breakdown`을 직접 사용하지 않아 화면 깨짐은 없으나 타입 불일치 상태 |
| 33 | API 타임아웃 미설정 | 경고 | axios 호출에 timeout 미지정 — 응답 없으면 무한 로딩 |

---

### `frontend/src/components/SearchBar.tsx`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 34 | 주소 검색 → 좌표 변환 | PASS | Geocoder → Places 순서 fallback 정상 |
| 35 | setSearching(false) 처리 | PASS | geocoder 성공(line 59), places 콜백(line 55) 양쪽 처리됨 |
| 36 | Enter 키 검색 | PASS | onKeyDown 정상 처리 |
| 37 | 검색 중 버튼 비활성화 | PASS | `disabled={searching}` 정상 |

---

### `frontend/src/components/MapViewer.tsx`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 38 | 마커 클러스터링 | PASS | 최근 세션에서 수정 완료 |
| 39 | 지도 클릭 → 출발지 설정 | PASS | coord2Address 역지오코딩 정상 |
| 40 | **any 타입 남용** | **BUG** | `useState<any>`, `useRef<any>`, 카카오 콜백 전부 `any` — Kakao Maps 타입 정의 없음. 런타임 에러 위험 |
| 41 | clusterer 미정리 | 경고 | useEffect cleanup에서 `clusterer.clear()`만 호출, `clusterer.setMap(null)` 없음 |

---

### `frontend/src/components/AttractionList.tsx`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 42 | 관광지 목록 렌더링 | PASS | key, onClick 정상 |
| 43 | hover 스타일 | 경고 | `e.currentTarget.style.background` 직접 DOM 조작 — CSS hover 클래스 권장 |

---

### `frontend/src/app/page.tsx` / `layout.tsx`

| # | 항목 | 결과 | 내용 |
|---|------|------|------|
| 44 | 상태 관리 구조 | PASS | attractions, origin, selectedAttraction 구조 적절 |
| 45 | Kakao JS SDK 로드 | PASS | layout.tsx Script 컴포넌트로 정상 로드 |
| 46 | NEXT_PUBLIC_KAKAO_JS_KEY 환경변수 | PASS | .env.local 분리 처리 |

---

## 버그 목록 (수정 대상)

| 우선순위 | 파일 | 내용 |
|----------|------|------|
| 🔴 HIGH | `routeService.ts:15` | BUSAN_BIMS_KEY 하드코딩 → .env 이동 |
| 🔴 HIGH | `app.ts:67-70` | parseFloat NaN 미검증 → 유효성 검사 추가 |
| 🟡 MED | `ScorePanel.tsx:13-20` | breakdown 인터페이스 타입 불일치 → 백엔드 실제 응답과 일치시킴 |
| 🟡 MED | `MapViewer.tsx` | any 타입 — Kakao Maps 미지원으로 불가피하나 주석으로 명시 권장 |

---

## 수정 완료 내역 (이번 세션)

| 파일 | 내용 |
|------|------|
| `routeService.ts` | BusanBIMS 실시간 대기시간 연동 (A 작업 완료) |
| `scoreAccessibility.ts` | `장애인화장실` 공백 없는 variant 추가 |
| `seed.ts` | accessScore DB 반영 |
| `app.ts` | DB accessScore 실제 조회 반영 |
