# TransitScore 전체 코드 점검 보고서

> **점검 일시**: 2026-03-19
> **점검 방식**: 소스코드 정적 분석 (수동 코드 리뷰)
> **점검 범위**: 백엔드(app.ts, score.ts, routeService.ts) + 프론트엔드(page.tsx, MapViewer.tsx, ScorePanel.tsx, SearchBar.tsx) + 문서(알고리즘.md)

---

## 1. 백엔드 (backend/src/)

### 1-1. score.ts (점수 알고리즘)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | 시간 점수 정규화 공식 (20~120분 선형) | ✅ | clamp(0~1) 처리 정상 |
| 2 | 환승 계단형 패널티 (0/1/2/3/4+ 회) | ✅ | 0.75 → 0.5 → 0.25 → 0.1 |
| 3 | 도보 점수 정규화 (D_max=1200m) | ✅ | clamp(0~1) 처리 정상 |
| 4 | 대기 점수 정규화 (W_max=20분) | ✅ | clamp(0~1) 처리 정상 |
| 5 | 가중합 (45%+20%+15%+10%+10%=100%) | ✅ | 합산 비율 정확 |
| 6 | `accessScoreFactor` 파라미터 유연성 | ✅ | 기본값 0.5, 외부 주입 가능한 구조 |
| 7 | 결과 소수점 처리 (finalScore 1자리) | ✅ | `toFixed(1)` 적용 |

**발견된 이슈 없음.**

---

### 1-2. routeService.ts (ODsay 경로 탐색)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | ODsay API 키 환경변수 검증 | ✅ | undefined 시 즉시 에러 throw |
| 2 | ODsay 응답 에러 처리 (data.error) | ✅ | Array/Object 형태 모두 처리 |
| 3 | 환승 횟수 계산 (bus + subway - 1) | ✅ | 최소 0 보장 처리 |
| 4 | Fallback 발동 조건 (에러 시 자동) | ✅ | try-catch로 모든 실패 포착 |
| 5 | 하버사인 거리 계산 정확성 | ✅ | 표준 공식 정상 구현 |
| 6 | Fallback 환승 추정 (10km/20km 기준) | ✅ | 거리 구간별 0/1/2회 추정 |
| 7 | Fallback 최소 시간 보장 (`Math.max(5,...)`) | ✅ | 0분 방지 처리 |
| 8 | `waitTimeMin` 실제 배차 간격 반영 | ⚠️ | ODsay 무료 플랜 제약 — 고정 5분 하드코딩. 알려진 제약사항 |

---

### 1-3. app.ts (Express API)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | `/health` 헬스체크 응답 형식 | ✅ | `status`, `message`, `timestamp` 포함 |
| 2 | `/api/attractions` DB→mockData 폴백 | ✅ | DB 비어있을 시 mockAttractions 반환 |
| 3 | `/api/score/:id` 파라미터 검증 | ✅ | 4개 좌표 누락 시 400 반환 |
| 4 | `parseFloat` 좌표 변환 | ✅ | 문자열→숫자 정상 변환 |
| 5 | `routeResult.success`가 false일 때 502 처리 | ⚠️ | Fallback 로직이 항상 `success: true`를 반환하므로 이 분기는 실제로 도달 불가능한 dead code |
| 6 | DB의 `Attraction.accessScore` 점수 계산 반영 | ❌ | **미구현**: `attractionId`로 DB 조회 후 `accessScore`를 `calculateAccessibilityScore`의 두 번째 인자로 넘겨야 하지만, 현재 DB 조회 없이 기본값 0.5만 사용됨 |

---

## 2. 프론트엔드 (frontend/src/)

### 2-1. page.tsx (메인 상태 관리)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | 상태 관리 구조 (attractions / filtered / selected) | ✅ | 관심사 분리 명확 |
| 2 | `handleAttractionsLoaded` useCallback 처리 | ✅ | MapViewer useEffect 무한루프 방지 |
| 3 | `handleSearch` useCallback + attractions 의존성 | ✅ | 검색 필터 정상 |
| 4 | `handleOriginChange` 시 `selectedAttraction` 초기화 | ✅ | 출발지 변경 → 패널 자동 닫힘 |
| 5 | 기본 출발지 (부산역 좌표) 하드코딩 | ✅ | 의도된 초기값 |

---

### 2-2. MapViewer.tsx (지도 컴포넌트)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | 관광지 데이터 API 호출 (onMount 1회) | ✅ | `onAttractionsLoaded` useCallback 덕분에 1회만 실행 |
| 2 | `selectedAttraction` 변경 시 지도 중심·레벨 이동 | ✅ | level 4로 줌인 |
| 3 | MarkerClusterer 재생성 시 기존 clear() | ✅ | `clustererRef`로 이전 인스턴스 제거 |
| 4 | 지도 클릭 → 역지오코딩 → 출발지 설정 | ✅ | road_address 우선, 없으면 address 사용 |
| 5 | 주소 12자 초과 시 말줄임 처리 | ✅ | `slice(0, 12) + '...'` |
| 6 | `kakao.maps.services` 미로드 시 좌표 fallback | ✅ | `window.kakao?.maps?.services` optional chaining |
| 7 | 클러스터러 useEffect cleanup (return 문) | ✅ | 언마운트 시 `clusterer.clear()` 호출 |
| 8 | `onMarkerClick`이 useEffect dependency에 없음 | ⚠️ | `attractions`가 변경될 때마다 클러스터러 재생성 시 최신 `onMarkerClick` 반영됨 — 현재 동작에는 문제없으나 ESLint exhaustive-deps 경고 가능성 |

---

### 2-3. ScorePanel.tsx (점수 패널)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | 점수 API 호출 조건 (attraction + origin 의존성) | ✅ | id, lat, lng 변경 시 재호출 |
| 2 | 로딩/에러/성공 상태 분기 | ✅ | 3단계 UI 정상 |
| 3 | Fallback 배너 표시 (isFallback 플래그) | ✅ | rawParams.isFallback으로 조건부 렌더 |
| 4 | 원형 게이지 dashoffset 계산 | ✅ | circumference 기반 정확한 수식 |
| 5 | 등급 판별 (80/60/40 기준) | ✅ | 문서의 점수 해석표와 일치 |
| 6 | `breakdown` 필드명 불일치 (잠재 버그) | ⚠️ | 백엔드 `breakdown`의 키: `s_time`, `s_transfer`, `s_walk`, `s_wait` / 프론트 `ScoreDetails.breakdown` 타입 정의: `timeScore`, `transferScore`, `walkScore`, `waitScore` — 현재 화면에 `breakdown`을 직접 렌더링하지 않아 런타임 오류는 없지만, 향후 상세 점수 표시 기능 추가 시 버그 발생 |

---

### 2-4. SearchBar.tsx (검색 + 출발지 설정)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | 관광지 검색 (이름 + 주소 필터링) | ✅ | 대소문자 무시 처리 |
| 2 | ✕ 버튼 클릭 시 검색어 초기화 | ✅ | `onSearch('')` 호출 |
| 3 | 주소 Geocoder 검색 | ✅ | 정상 |
| 4 | Geocoder 실패 시 Places API 폴백 | ✅ | 장소명 검색 대체 |
| 5 | Enter 키 검색 | ✅ | `onKeyDown` 이벤트 처리 |
| 6 | 검색 버튼 중복 클릭 방지 | ✅ | `searching` 상태로 disabled 처리 |
| 7 | `setSearching(false)` 누락 케이스 | ⚠️ | Geocoder 성공 경로에서 `setSearching(false)`는 콜백 마지막에 실행되나, `kakao.maps.services` 자체가 없는 환경(SSR 등)에서는 `setSearching(false)`가 호출되지 않아 버튼이 영구 비활성화될 수 있음 — 현재 `'use client'` 환경에서는 발생하지 않음 |

---

## 3. 문서 (document/알고리즘.md)

| # | 점검 항목 | 결과 | 비고 |
| :--- | :--- | :---: | :--- |
| 1 | 시간 점수 공식 (코드와 일치) | ✅ | |
| 2 | 환승 패널티 수치 (코드와 일치) | ✅ | |
| 3 | 도보 점수 기준 D_max=1200m (코드와 일치) | ✅ | |
| 4 | 대기 점수 기준 W_max=20분 (코드와 일치) | ✅ | |
| 5 | 가중치 표 (코드와 일치) | ✅ | |
| 6 | Fallback 로직 설명 (코드와 일치) | ✅ | 하버사인, 15km/h, 15% 도보 |
| 7 | 최종 수식의 $S_{access}$ 표현 | 🔧 | **수정 완료**: 기존 `0.10 * 0.5` 하드코딩 → `0.10 * S_{access}` + 기본값 설명 추가 |

---

## 4. 종합 결과

| 카테고리 | 항목 수 | PASS | 경고(⚠️) | 실패(❌) |
| :--- | :---: | :---: | :---: | :---: |
| score.ts | 7 | 7 | 0 | 0 |
| routeService.ts | 8 | 7 | 1 | 0 |
| app.ts | 6 | 4 | 1 | 1 |
| page.tsx | 5 | 5 | 0 | 0 |
| MapViewer.tsx | 8 | 7 | 1 | 0 |
| ScorePanel.tsx | 6 | 5 | 1 | 0 |
| SearchBar.tsx | 7 | 6 | 1 | 0 |
| 문서(알고리즘.md) | 7 | 6 | 0 | 0 (수정 완료) |
| **합계** | **54** | **47** | **5** | **1** |

---

## 5. 발견된 이슈 정리

### ❌ 버그 (수정 필요)

| 파일 | 내용 |
| :--- | :--- |
| `backend/src/app.ts` | `/api/score/:id` 엔드포인트에서 `attractionId`로 DB를 조회하지 않아 `Attraction.accessScore`가 점수 계산에 전혀 반영되지 않음. `calculateAccessibilityScore(routeResult, attraction.accessScore)` 형태로 수정 필요 |

### ⚠️ 경고 (잠재적 문제 / 인지 필요)

| 파일 | 내용 |
| :--- | :--- |
| `backend/src/app.ts` | `routeResult.success === false` 분기는 도달 불가능한 dead code (Fallback이 항상 `success: true` 반환) |
| `backend/src/routeService.ts` | ODsay 무료 플랜 제약으로 `waitTimeMin` 고정 5분 — 알려진 제약사항 |
| `frontend/src/components/ScorePanel.tsx` | `breakdown` 필드명이 백엔드와 불일치 — 현재는 화면에 미사용이라 무해하나 향후 버그 가능성 |
| `frontend/src/components/MapViewer.tsx` | `onMarkerClick`이 클러스터러 useEffect dependency 미포함 — ESLint 경고 가능성, 동작은 정상 |
| `frontend/src/components/SearchBar.tsx` | SSR 등 `kakao.maps.services` 미로드 환경에서 `setSearching(false)` 미호출 가능성 — 현재 환경에서는 무해 |

---

## 6. 다음 권장 작업

1. **[우선]** `app.ts`에서 `attractionId`로 DB 조회 후 `accessScore` 실제 반영
2. **[중간]** `ScorePanel.tsx`의 `breakdown` 타입을 백엔드(`s_time` 등)와 일치시킴
3. **[낮음]** `app.ts`의 dead code 분기 제거
